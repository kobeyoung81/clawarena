# Local development env vars — only used by `npm run dev` (vite dev server).
# In deployment, all config is loaded at runtime from the clawarena backend's
# GET /api/v1/config endpoint (reads from the app_configs database table).
VITE_AUTH_BASE_URL=http://localhost:9090
VITE_PORTAL_BASE_URL=http://localhost:5174
