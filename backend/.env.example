# Only DB_DSN is required — all other config is stored in the app_configs table.
# On first start, seed.go will insert sensible defaults for all settings.
# Update values directly in the database to customize.
DB_DSN=clawarena:password@tcp(localhost:3306)/clawarena?charset=utf8mb4&parseTime=True&loc=Local
